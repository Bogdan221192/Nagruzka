import os
import sys
if len(sys.argv) == 2:
    raw_path = sys.argv[1] #получается аргумент командной строки
    path = raw_path.replace('\\', '/').replace('"',"") #\ меняется на /, убираем ""
    f = open(path, 'r')
    info = f.read()
    f.close()
    print("Содержимое файла\n", info)
else:
    print("Не указан путь!")

os.system("pause")
from statistics import median


content = f.read()
file_contents = [int(x) for x in content.split()]
print(file_contents)

a = file_contents
m = median(a)
moves = (sum(abs(v - m) for v in a))
if moves > 20:
    print ("Невозможно сделать за 20 ходов")
else:
    print (moves)